{{application-header}}

{{directory-picker dirs=dirs addDir="addDir"}}

{{#if loadedDirs.length}}

  {{search-bar query=query}}
  {{#each searchResults as |file|}}
    {{file.filename}}<br>
  {{/each}}

{{/if}}

{{outlet}}
